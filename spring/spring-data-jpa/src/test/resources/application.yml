spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        jdbc:
          batch_size: 50
        order_inserts: true
        # format_sql: true
#        generate_statistics: true

logging:
  level:
    com.p6spy: DEBUG
    # org.hibernate.SQL: debug
    # org.hibernate.loader.collection: debug
    # org.hibernate.type.descriptor.sql: trace
    # org.hibernate.SQL: DEBUG